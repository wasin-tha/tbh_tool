# Steam login token — ห้าม commit เด็ดขาด (ใช้กับ fetch_prices.py)
steam_cookie.txt

# raw price data (source) + build output — root/prices.json เท่านั้นที่ track (served)
tbh_march/tbh_prices.json
tbh_march/prices.json

# junk
__pycache__/
*.pyc
*.tmp
price_progress.json
.venv/
